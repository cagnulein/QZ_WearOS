name: Wear OS emulator test

on:
  push:
    branches:
      - claude/analisi-discussione-4q6gab
  pull_request:
  workflow_dispatch:

jobs:
  instrumented-test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Enable KVM group perms
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Remove hardcoded local JDK path from gradle.properties
        run: sed -i '/org.gradle.java.home/d' gradle.properties

      - name: Assemble debug APK
        run: ./gradlew :app:assembleDebug --stacktrace

      - name: AVD cache
        uses: actions/cache@v4
        id: avd-cache
        with:
          path: |
            ~/.android/avd/*
            ~/.android/adb*
          key: avd-wear-34

      - name: Create AVD snapshot for caching
        if: steps.avd-cache.outputs.cache-hit != 'true'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          target: android-wear
          arch: x86_64
          profile: wearos_small_round
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          script: echo "AVD created"

      - name: Run instrumented test on Wear OS emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          target: android-wear
          arch: x86_64
          profile: wearos_small_round
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          disable-animations: true
          script: |
            adb logcat -c
            adb logcat *:E AndroidRuntime:E ExerciseService:D -v time > logcat.txt &
            LOGCAT_PID=$!
            ./gradlew :app:connectedDebugAndroidTest --stacktrace
            TEST_EXIT=$?
            # Screenshot taken by the test itself while the service was still in the
            # foreground - this is the real proof, pulled from app-private internal storage
            # (external storage isn't reliably mounted on this Wear OS emulator image).
            adb pull /data/data/org.cagnulen.qdomyoszwift/files/ongoing_activity_screenshot.png ongoing-activity-screenshot.png || true
            # Screenshot taken after the whole test process/teardown has finished, kept
            # only for comparison/debugging - the service is expected to be gone by now.
            adb exec-out screencap -p > emulator-screenshot-after-teardown.png || true
            kill $LOGCAT_PID || true
            exit $TEST_EXIT

      - name: Upload logcat
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: logcat
          path: logcat.txt

      - name: Upload ongoing activity screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ongoing-activity-screenshot
          path: ongoing-activity-screenshot.png

      - name: Upload post-teardown emulator screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: emulator-screenshot-after-teardown
          path: emulator-screenshot-after-teardown.png

      - name: Upload instrumented test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: instrumented-test-report
          path: app/build/reports/androidTests/connected
